# 人間の認知限界を超克する多次元概念アーキテクチャ

――アリストテレス『オルガノン』の再実装による非決定論的現実と決定論的システムの調停――

## 要旨（Abstract）

本稿では、非決定論的で文脈依存の現実を、決定論的な論理・プログラムの世界へ安全に受け渡す仲介層を提案する。生成AI（LLM）による自然言語処理と、オントロジー・推論エンジンによる構造的検証の間に、アリストテレス『オルガノン』の考え方を再実装した保証層を置く。『カテゴリー論』による実体と付帯性の区別、『命題論』による様相と時制、『トポス論』によるFactとEndoxaの分離、『詭弁反駁論』による誤謬検知を通じて、曖昧な根拠を計算可能な命題へ翻訳する。AWS Context Ontology Accelerator（COA）のような既存基盤とは競合せず、意味・認識論・誤謬検知を担う補完層として接続する。

---

## 1. 序論：非決定論的現実と決定論的システムの調停

現実の業務文書、問合せ、議事録、DWHのデータは、時間、立場、適用範囲、証拠の強さによって意味が変わる。そこにはFactだけでなく、Endoxa、予定、可能性、観測報告、誤謬が共存する。一方、データベース、近代的な述語論理、推論プログラムは、対象と関係が定義された構造を前提とする。この差を無視して自然言語を直接決定論的な知識グラフへ変換すると、誤った一般化、名辞のすり替え、古い仕様の混入を「事実」として固定してしまう。

本ツールの役割は、現実を単純化して消去することではない。根拠、文脈、不確実性、異論を保持したまま、決定論的な後段が扱える命題へ翻訳し、どこまで確定できるかを明示することである。LLMは仮説生成器、organon-utilは意味論的な翻訳・保証層、OWL/DL推論器は必要に応じた大域的な構造検証器として位置付ける。

## 2. 理論的アプローチ：非決定論と決定論を繋ぐアリストテレス的仲介プロセス

自然言語から計算可能なオントロジーを抽出するため、古典的論理学を「分類・翻訳・保留・検証」のパイプラインとして実装する。

1. **命題化：** 自然言語を主語・述語・目的語へ分解し、原文と出典を保持する。
2. **カテゴリー化：** 実体・概念・付帯性を区別し、`is-a` と属性・関連を同一視しない。
3. **命題論：** A/E/I/Oに加え、`MUST` / `MAY` / `ACTUAL` の様相と `PRESENT` / `FUTURE` の時制を付与する。
4. **認識論的分類：** Fact、Endoxa、Fallacyを分離し、確度、根拠、権威性、適用期間を別軸で管理する。
5. **決定論への受け渡し：** 検証対象を明示的に選び、承認済みの命題だけをOWL、SHACL、SQL、または他の決定論的処理へ渡す。

## 3. 保証層（Assurance Layer）：『トポス論』と『詭弁反駁論』の実装

LLMが生成した仮説的構造を、いきなり真偽の二値へ押し込まない。保証層は、根拠と文脈を保持したまま、論理的に扱える命題、保留すべき命題、誤謬を含む命題へ分岐させる。

* **同一律：** 名寄せとURIの統一により、同じ名辞が文脈ごとに別物へすり替わることを防ぐ。
* **矛盾律：** 同一条件・同一時点・同一対象について排反するFactを検出する。ただし、条件や時点が違うEndoxaは直ちに矛盾としない。
* **排中律の限定適用：** 閉世界の業務ルールやSHACLで網羅性を検証する。開世界の知識に対して「未登録だから偽」とは扱わない。
* **詭弁反駁：** 曖昧語の虚偽、早まった一般化、権威だけに依存する論証、誤った換位を検出し、Fallacyとして保留または差し戻す。

### 3.1 トポス・レジストリ：判断観点の体系的管理

『トピカ』に登場するトポスは、固定された個数の完全なルール集として扱わない。研究上も分類や数え方には幅があり、業務領域によって必要な判断観点も異なるため、本システムでは**トポス・レジストリ**を版管理された拡張可能な知識資産として保持する。

トポスは命題を直接「真」と判定する規則ではなく、どの観点から命題を検討し、どの前提や反例を確認すべきかを示す。実装上は、決定論的に検証できるもの、LLMの候補生成を必要とするもの、人間の判断を必要とするものを区別する。

```yaml
topoi:
  - topoi_id: genus_species_scope
    name: 類と種の適用範囲
    category: scope
    premise_pattern:
      predicate: is_a
    conclusion_pattern:
      relation: scope_contains
    risk:
      - illicit_generalization
    explanation: "類は種より広い適用範囲を持つ"
    automation_level: deterministic

  - topoi_id: definition_consistency
    name: 定義の一貫性
    category: definition
    check:
      same_term_same_context: true
    explanation: "同じ名辞を推論中に異なる意味で使用していないか確認する"
    automation_level: assisted
```

初期カテゴリは次のように整理する。

* `scope`: 類・種、上位・下位、適用範囲
* `definition`: 定義、同義性、名辞の一貫性
* `relation`: 相反、対立、因果、比較
* `property`: 固有性、偶有性、属性
* `argument`: 前提から結論への論証形式
* `exception`: 例外、条件、反例

例えば「類は種より広い」というトポスは、`仕様書 is_a 文書` から、文書の適用範囲が仕様書の適用範囲を含むという検討観点を与える。ただし、類に属する性質がすべての種へ自動継承されるわけではない。必要条件、十分条件、量化子、適用範囲を別途確認し、特称命題から全称命題へ拡張する場合は誤謬候補とする。

トポスの検出結果は、命題の真偽そのものと混同しないよう、次の形式で保持する。

```json
{
  "topoi_id": "genus_species_scope",
  "applied_to": ["prop-000105", "prop-000106"],
  "assessment": "scope_mismatch_candidate",
  "explanation": "種に関する特称命題を類全体へ拡張している可能性がある",
  "required_review": true
}
```

### 3.2 詭弁反駁レジストリ：誤謬パターンの管理

トポスが「検討の観点」を提供するのに対し、詭弁反駁レジストリは、論証のどこに誤謬の疑いがあるかを管理する。したがって、詭弁反駁にも独立した**Fallacy Registry**が必要である。これも固定的な一覧ではなく、誤謬の定義、検出パターン、必要な証拠、対応方針を版管理する。

```yaml
fallacies:
  - fallacy_id: equivocation
    name: 曖昧語の虚偽
    category: ambiguity
    detection:
      same_term_multiple_meanings: true
    action: pending_review
    explanation: "同じ名辞の意味が論証の途中ですり替わっている"

  - fallacy_id: hasty_generalization
    name: 早まった一般化
    category: scope
    detection:
      particular_to_universal: true
    action: downgrade_or_request_evidence
    explanation: "少数の事例から全体の性質を断定している"

  - fallacy_id: ad_verecundiam
    name: 権威論証
    category: evidence
    detection:
      authority_without_independent_evidence: true
    action: keep_as_endoxa
    explanation: "権威者の発言だけを根拠に事実化している"
```

Fallacyの判定は、LLMだけで確定させない。LLMは候補と理由を生成し、プログラム側で証拠数、命題の量化子、名辞の使用箇所、情報源の独立性を検査する。判定が確定できない場合は、命題を削除せず `Fallacy` または `pending` として保持し、根拠とともにHITLへ送る。

トポスとFallacyの関係は次のように整理する。

```text
Topos Registry
  → どの観点で検討するかを提示

Fallacy Registry
  → どの誤謬パターンが疑われるかを提示

Assurance Layer
  → 決定論的な検証、保留、差し戻しを実行

HITL
  → 文脈的・組織的な妥当性を判断
```

実装は、まず類と種の適用範囲、定義の一貫性、曖昧語、早まった一般化、権威論証、因果と相関の混同など、頻出かつ形式化しやすい項目から始める。その後、実データで検出された論証パターンをレジストリへ追加し、レジストリ自体も承認対象として管理する。

### 3.3 RDF / OWLと認識論的状態の責務分離

RDFやOWLは、命題、クラス、関係、制約を表現・推論するための標準であるが、`Fact`、`Endoxa`、`Hypothesis`という本ツールの認識論的・運用的状態を、それ自体で自動判定するものではない。RDF-starやReificationは命題自体へ出典・発話者・確度などを付与でき、Named Graphは文脈やテナントごとに命題集合を分離できるが、どの状態に分類し、どの推論経路へ送るかはorganon-utilの責務とする。

```text
RDF-star / Reification
  -> 命題単位の出典・状態・確度を表現

Named Graph
  -> Core / Context / Hypothesis等の文脈を分離

OWL / OWL RL / OWL DL
  -> 選択された命題集合の構造的帰結を推論

organon-util
  -> 認識論的分類、ルーティング、承認、履歴を管理
```

この分離により、Endoxaを単なる確率値へ還元せず、「特定の立場・時点・共同体で受容されているが、組織のFactとしては未承認」という状態として保持できる。

## 4. アーキテクチャの統合：Neuro-Symbolicループ

LLMを「仮説生成器」、organon-utilを「意味論的な仲介・保証層」として配置する。局所的な検証結果は、詭弁の種類、適用範囲、根拠不足などの説明付きレポートに変換してLLMへ返す。再試行回数には上限を設け、無限に事実を書き換えず、解決しないものはHITLへ送る。

必要な場合だけ、承認済みのFactをOWL/DL推論器へ渡して大域的な整合性を確認する。HermiTは選択肢の一つであり、organon-utilの必須依存にはしない。小規模なPoCではorganon-util単独の決定論的検証を使い、OWL 2 DLの大域推論が必要な環境ではHermiT、ELK、その他の適合する推論器をアダプター経由で利用する。

### 4.1 EndoxaとHypothesisの区別

EndoxaとHypothesisは、いずれも現時点で確定Factではなく、厳格なFact推論から分離される点では近い。しかし、その成立理由と次のアクションは異なる。

* **Endoxa:** 現場、専門家、部門、共同体で自然に受容されている意見・評価・報告。必ずしも検証を目的としていない。
* **Hypothesis:** 観測された事象を説明するため、反証・実証を目的として意図的に形成された検証可能な命題。

したがって、`epistemic_status`は未確定性と承認状態を表し、`claim_type`は `observation`、`opinion`、`hypothesis`、`recommendation`などの命題種別を表す。Hypothesisには、根拠命題、生成理由、検証方法、棄却条件を追加で保持する。

```json
{
  "epistemic_status": "Endoxa",
  "claim_type": "hypothesis",
  "derived_from": ["prop-000105", "prop-000106"],
  "rationale": "複数の障害報告と共通依存先から原因候補を形成した",
  "verification_method": "トランザクションログを確認する",
  "falsification_condition": "障害時刻にロック競合が存在しない"
}
```

### 4.2 仮説形成と科学的検証への応用

Neuro-Symbolicループは、既存命題の修正だけでなく、アブダクションから演繹的反証、現実世界での実証へ進む問題解決プロセスにも利用できる。

1. **観測の集約（Observation）:** 問合せチケット、ログ、DWHの観測値から事象を収集し、出典付きのObservationまたはEndoxaとして保持する。
2. **仮説形成（Abduction）:** LLMは関連するFactと複数の観測を根拠に、事象を説明できるHypothesis候補と検証方法を生成する。生成物はFactへ自動昇格させない。
3. **演繹的反証（Deduction / Falsification）:** Assurance Layerは、既存Factとの矛盾、Toposの適用範囲、Fallacy、検証不能性を評価し、成立しない仮説を棄却または修正要求へ送る。
4. **実証（Verification）:** 論理的に残った仮説について、MCP経由の観測、実験、ログ確認、HITLによって追加証拠を得る。
5. **状態遷移:** 十分な証拠と承認を得た命題は適用範囲・有効期間付きFactへ昇格し、反証された仮説も削除せず棄却理由とともに履歴へ残す。

この機能は「真理を自動発見した」と断定するものではない。システムは仮説候補、反証可能性、検証手順、根拠を構造化し、人間または外部観測による実証を支援するScientific Discovery Workflowとして位置付ける。

## 5. 概念実証（PoC）の実装アーキテクチャ

Pythonエコシステムを用いた具体的なパイプライン設計。原本・チャンク・ベクトル検索は `document-search-util` に委譲し、organon-utilは共通の `SourceRecord` を受け取る。これにより、ファイルシステム、Snowflake、Denodo、チケット管理システムを同じ命題化パイプラインへ接続できる。

* **源泉・検索層：** `document-search-util` が原本、チャンク、メタデータ、Embedding、検索、権限、DWH接続を担当する。
* **仲介・抽出層：** LLMとMCPを用いて命題を抽出し、`SourceRecord` の `logical_id`、`source_uri`、`checksum`、取得日時を根拠として保持する。
* **保証層：** `organon-util` がカテゴリー、様相、時制、Fact / Endoxa / Fallacy、誤謬説明、承認・履歴を管理する。
* **推論・制約層：** 標準経路ではPure Pythonの `rdflib`、`owlrl`（RDFS / OWL 2 RL）、`pySHACL`（制約検証）を利用する。OWL 2 DLの大域推論が必要な場合だけ、Owlready2経由のHermiT等をアダプターとして追加する。
* **回答層：** Fact、Endoxa、推論、推奨、不明情報を分離し、出典付きの構造化回答と自然文を生成する。
* **オーケストレーション：** LangGraph等で抽出、グラウンディング、局所保証、任意の大域推論、承認を循環制御する。

### 5.1 COAとの関係：競合ではなく仲介層としての位置付け

AWS Context Ontology Accelerator（COA）は、データソースの接続、オントロジーの生成、R2RMLによる構造化データとの対応、SHACLや推論器による検証、グラフ・ベクトル検索、MCPによる提供までを含む実装基盤である。特に、構造化データと文書を同一の利用体験へ接続し、承認済みのオントロジーを用いて検索・クエリを提供する点に強みがある。

本ツールはCOA全体を再実装するものではない。COAが提供する `Scan -> Model -> Serve` の流れに対し、organon-utilは次の不足を補う仲介層として位置付ける。

* 自然言語の命題を、実体・付帯性・様相・時制付きの構造へ翻訳する。
* Fact、Endoxa、Fallacyを分離し、現場の異論や不確実性を破棄せずに保留する。
* 『詭弁反駁論』に基づき、曖昧語、早まった一般化、権威だけに依存する主張などを説明付きで差し戻す。
* COAや他の推論器が返す構造的エラーを、LLMや人間が修正可能な理由へ翻訳する。

したがって、構成は次の二通りが考えられる。

```text
非構造化データ -> organon-util（命題化・認識論的分類）
                 -> COA（OWL/R2RML/SHACL/グラフ提供）

COAのScan/Model -> organon-util（追加保証・詭弁検知・差し戻し）
                 -> COAの承認・Serve
```

COAのHermiTを必ず置き換えることを前提にはしない。organon-util単独で完結する局所検証と、HermiT等を使う大域的なOWL整合性検証は異なる責務であり、後者は必要な環境だけで選択する。これにより、Python内で完結するPoCから、COAやNeptuneを利用するエンタープライズ構成まで、同じ命題契約を維持できる。

### 5.2 実装ロードマップ

新コンセプトを実装する際は、次の順序で段階化する。

1. **命題モデルの拡張：** `modality`、`tense`、`claim_type`、`fallacy_details`、`proposition_id`、原本位置情報に加え、Hypothesisの `derived_from`、`verification_method`、`falsification_condition`を追加する。
2. **トポス／Fallacyレジストリ：** 判断観点と誤謬パターンをYAML等で版管理し、カテゴリ、適用条件、検出結果、対応方針を定義する。
3. **決定論的な局所保証：** Fact / Endoxa / Fallacyのルーティング、名寄せ、適用範囲、矛盾候補、レジストリに基づく誤謬説明を実装する。
4. **LLMアダプター：** `LLMConfig`を用いて、抽出・分類・回答生成をJSONスキーマ付きで呼び出す。LLM未設定時はルールベースへフォールバックする。
5. **MCPグラウンディング：** 未知語のlookupとルール照会を許可済みツールとして提供し、取得結果に出典・権限・有効期限を付与する。
6. **SourceRecord連携：** `document-search-util`のファイル、DWH、Denodo検索結果を命題化し、`source_uri`、`checksum`、`retrieved_at`、データ系譜を保持する。
7. **任意の大域推論：** 承認済みのFactだけをOWL/DL推論器へ渡し、結果を保証レポートとして保存する。推論器はHermiTに固定しない。
8. **仮説検証ワークフロー：** Observation / EndoxaからHypothesisを形成し、反証・追加観測・HITL・昇格・棄却の履歴を管理する。
9. **回答生成：** Fact、Endoxa、Hypothesis、推論、推奨、不明を分離し、引用IDを検証して構造化回答と自然文を返す。

このロードマップにより、最初からCOA相当の巨大な基盤を構築するのではなく、現在のPython PoCを検証しながら、必要な箇所だけを外部基盤へ接続できる。

### 5.3 主要な状態と受け渡し契約

処理中の命題は、少なくとも以下の状態を持つ。

```text
extracted -> grounded -> classified -> locally_validated
          -> proposed -> approved / rejected / pending
          -> projected / invalidated

observation / endoxa -> hypothesis
                     -> falsified / verification_pending
                     -> verified -> approved_fact
```

各命題は `SourceRecord` の `logical_id` と `source_uri` に戻れる必要がある。COA、document-search-util、organon-utilの間で共有するのは内部DBのテーブルではなく、この出典付き命題契約とする。

## 6. 本アーキテクチャにおける技術的課題

システム実装に向けた三大障壁の整理。

1. **言語と意味の壁：** 「ポロンちょ」のような未知語に対する実体/付帯性の判定不能（記号接地問題）、および日常言語における暗黙的量化子（A/E/I/O）の決定困難。
2. **規模と計算の壁：** 巨大文書のチャンキングに伴う文脈分断（代名詞の迷子）と、OWL推論エンジンの計算量爆発（EXPTIME）。
3. **システム制御の壁：** LLMがエラーを消すために事実を曲げる「過剰適応」と、複数文書間での名寄せの困難。

## 7. 課題解決のアプローチ：MCPによる動的セマンティック・グラウンディング

第6章の課題（特に記号接地と量化子の問題）を突破するための具体策。

* **外部マスターデータの動的注入：** Model Context Protocol (MCP) を実装し、LLMが未知のエンティティに直面した際、自律的に組織のオントロジーへルックアップを行う機構の導入。
* **ルール・プロキシによる決定論的補正：** 「仕様書は承認必須」等のドメイン規約をMCP経由で参照させることで、確率的な推測を排除し、全称/特称の論理マッピングをシステム的に確定させる。

## 8. 結論と今後の展望：古代の認識論と生成AIの交差

### 8.1 総括：認識論的な仲介層としてのorganon-util

本稿では、非決定論的で文脈依存の現実と、決定論的な計算・推論システムの間を調停する認識論的な仲介層を定義した。『カテゴリー論』と『命題論』によって実体・付帯性・様相・時制を構造化し、『トピカ』に基づくTopos Registryによって検討観点を提示し、『詭弁反駁論』に基づくFallacy Registryによって論証上の危険を検出する。

RDF-star、Named Graph、OWL、SHACLは、この認識論的状態を記録・分離・検証するための標準技術として利用する。ただし、Fact / Endoxa / Hypothesis / Fallacyの分類と状態遷移は、それらの規格が自動的に与えるものではなく、organon-utilが出典、文脈、検証結果、承認履歴を用いて管理する。

### 8.2 なぜ今このアプローチが実装可能なのか

従来のルールベースNLPでも構文解析や限定的な情報抽出は可能だったが、発話者の立場、評価表現、含意、仮説性などを多様な文書から汎用的に抽出するには多くの個別ルールを必要とした。LLMは、この文脈解釈を担う柔軟な認知パーサーとして、アリストテレス的な分類を実用規模で候補生成できるようにした。

ただし、LLMの分類は確率的な提案であり、真理判定ではない。本アーキテクチャはLLMを権威として用いず、構造化出力、原文引用、決定論的検証、Topos / Fallacy評価、MCPグラウンディング、HITLによって制約する。生成AIの読解・仮説形成能力を利用しつつ、その出力を検証可能な対象に変換することが技術的な要点である。

### 8.3 知識管理から科学的問題解決への拡張

EndoxaとHypothesisを区別して管理することで、本ツールは静的な知識整理だけでなく、観測から仮説を形成し、既存Factによる反証と現実世界の実証を支援する問題解決基盤へ拡張できる。仮説は検証方法と棄却条件を持ち、成功した仮説だけが適用範囲・時点・根拠付きのFact候補となる。この履歴は、採用された知識だけでなく、棄却された説明とその理由も組織知として保存する。

### 8.4 今後の展望

本稿で提示した理論的枠組みおよび要件定義を実社会のエンタープライズ環境で実稼働させるためには、以下の領域におけるさらなる技術的検証と研究の深化が必要である。

1. **チャンキング分断の克服とGraphRAGの統合**
巨大なドキュメントを処理する際に生じる文脈の分断（代名詞の迷子等）を防ぐため、単純なベクトル検索だけでなく、命題抽出と並行して中間的な知識グラフを構築・参照するGraphRAG（Graph Retrieval-Augmented Generation）的アプローチの統合を検証する。これにより、複数文書間にまたがるエンティティ解決（名寄せ）の精度向上を図る。
2. **推論エンジンの計算量削減とハイブリッド検証アーキテクチャ**
OWL記述論理に基づく完全な推論は、ノード数の増加に伴い計算量が指数関数的に爆発するリスクを孕む。実運用に向けては、DuckDB等のOLAP処理で大まかな不整合を事前フィルタリングし、厳密な推論エンジン（HermiT等）の起動を疑わしいサブグラフのみに限定するような、計算リソースの最適化アーキテクチャを設計する必要がある。
3. **実データを用いた定量評価**
現在のPython PoCと固定評価セットを拡張し、実在する規程集、技術文書、チケット、DWH由来データを入力として、命題抽出のPrecision / Recall / F1、検索のRecall@K、引用精度、Fallacy検出率、仮説の反証・実証率を測定する。Assurance Layerを持たないベースラインとの比較によって、有効性と過剰拒否の双方を評価する。
4. **Scientific Discovery Workflowの検証**
障害対応などの限定領域で、Observationの集約、Hypothesis生成、決定論的反証、MCPによる追加観測、HITL承認までをE2Eで検証する。システムが生成した仮説を自動的な真理とせず、再現可能な検証手順と証拠を提示できるかを評価する。

人間の限界である一次元的認知を多次元へと拡張する壮大な試みは、まず「条件を統制した一次元ツリーの正確な自動構築」から始まる。本研究で整理したアリストテレス的論理基盤と最新のAIエージェント技術の融合は、未来のナレッジマネジメントにおける確固たる設計指針となるものである。

## 補足1. グラウンディングデータの作成・整備
MCP（Model Context Protocol）を通じてLLMに注入する「グラウンディング用データ（前提知識・マスターデータ）」をどのように作成・準備・運用するかは、このアーキテクチャの成否を分ける極めて実務的かつ重要なテーマです。

LLMが「ポロンちょ」のような未知の単語や、曖昧なビジネスルールに直面した際、的確な前提知識を返すためには、グラウンディング用データがシステム的に整理されている必要があります。

このデータの作成と運用は、「トップダウン（演繹的定義）」「ボトムアップ（既存資産の統合）」「自己増殖（システムからの還元）」の3つのフェーズで構築するのがベストプラクティスです。

---

### 1. トップダウンでの作成（人間による「類」とルールの定義）

システムの根幹となる「目的因（何のための分類か）」や、絶対に揺るがしてはならない最上位のオントロジー（最高位の類）は、人間（ドメインエキスパートやデータスチュワード）が明示的に作成する必要があります。

これらは、静的なYAMLやJSONファイルとして定義し、MCPサーバーの初期データセットとして読み込ませます。

* **コアカテゴリ（実体の定義）の作成:**
組織内に存在する「文書」「部署」「製品」「システム」などの最上位クラスと、その基本属性を定義します。
* **ビジネスルール（量化子の規約）の作成:**
アリストテレス論理の定言命題（A, E, I, O）を確定させるためのルールブックです。

**【グラウンディングデータの定義例（JSON）】**

```json
{
  "entities": [
    {
      "id": "urn:enterprise:concept:Specification",
      "name": "仕様書",
      "base_type": "Substance", // 実体であることを明示
      "parent_class": "Document"
    }
  ],
  "governance_rules": [
    {
      "target_entity": "urn:enterprise:concept:Specification",
      "rule_type": "quantifier_mapping",
      "condition": "requires_approval",
      "logical_form": "UniversalAffirmative", // 全称肯定 (All S is P)
      "description": "仕様書は例外なく承認を必要とする"
    }
  ]
}

```

これにより、LLMが「仕様書」という単語を見た瞬間、MCP経由で「これは実体であり、関連するルールは全称肯定である」という前提知識を得られます。

### 2. ボトムアップでの作成（既存のエンタープライズ資産からの統合）

すべての専門用語（数万に及ぶプロジェクト名やシステム名）を人間が手作業でYAMLに書くのは不可能です。ここで、企業内にすでに存在するデータ資産をMCPの知識源として統合（データフェデレーション）します。

エンタープライズ環境では、データが様々なサイロに散らばっています。これらを直接MCPで叩くのではなく、Denodoのようなデータ仮想化基盤（データファブリック）や、Unity Catalogのような統合データガバナンス層をセマンティックレイヤーとして活用し、MCPサーバーはそれらのAPIをラップする設計が非常に有効です。

* **RDBやデータウェアハウスのメタデータ:**
テーブル定義（DDL）の「テーブル名＝実体」「カラム名＝付帯性（プロパティ）」という関係性を抽出し、MCPの辞書データとして変換します。
* **Active Directory / 組織図API:**
「営業部」「開発部」といった組織ツリーをそのままオントロジーの `is-a` 関係として取り込みます。
* **作成の自動化パイプライン:**
夜間バッチ等でUnity Catalog等からメタデータを抽出し、RDF（Turtle）やベクトルDB（DuckDB等）に変換して、MCPサーバーが検索しやすいフォーマットでインデックス化します。

### 3. 自己増殖による作成（Assurance Layerからのフィードバック）

このシステムが最も真価を発揮するのは、「システム自身が稼働しながら、未知のデータを学習し、グラウンディングデータを自動生成・拡張していく」フェーズです。

前段で設計したLangGraphのワークフローにおいて、LLMの提案が推論エンジン（Assurance Layer）の論理チェックを通過し、「承認（Approved）」状態になったとします。

1. **新規概念の発見:** LLMが未知のドキュメントから「ポロンちょ」という新システム名を発見し、文脈から「システムモジュールの一種（実体）」として推論を提案する。
2. **論理検証:** 既存のルールと矛盾しないか、推論エンジンがチェックする。
3. **マスターデータの更新:** 矛盾がなければ、「ポロンちょ」を新たなエンティティとして、MCPサーバーが参照するバックエンドDB（DuckDB等）に自動的に書き込み（INSERT）ます。

次に別のドキュメントで「ポロンちょ」が出現した際、MCPサーバーは先ほど承認されたばかりの定義を「前提知識」としてLLMに返すことができます。つまり、**多次元ツリーの構築作業そのものが、グラウンディングデータを作成するプロセスと同一になる**のです。

---

### まとめ：MCPサーバーのアーキテクチャ実装

グラウンディングデータの作成と提供を担うMCPサーバーは、巨大な一枚岩のデータベースである必要はありません。
FastMCPなどの軽量なPythonフレームワークを用いて、以下のようなツール（Tools）をLLMに公開するプロキシ（仲介役）として実装するのが理想的です。

* `lookup_entity(term: str)`: 既存のマスターデータやUnity Catalogのメタデータから実体定義を引くツール。
* `get_domain_rule(entity_id: str)`: トップダウンで定義されたガバナンス規則（全称・特称の規約）を引くツール。

これにより、「人間が定める絶対的なルール」「既存のシステム資産」「AIが抽出した新たな知識」の3つが融合した、極めて強力なグラウンディング・データ基盤が完成します。

## 補足2. 命題の真偽判定
```
トポス論で言われているように、世の中の命題はエンドクサに基づくものが多く、パッと真なのか偽なのかが判断できないものが多いです。
このアプリでLLMで命題を抽出したとしてもそれが真であるか偽であるか判断つかないものがあるかと思いますが、それをどのように管理して真偽判別すればよいですか
```
まさにその通りです。アリストテレスの『トポス論（トピカ）』と「エンドクサ（通念・広く受け入れられている意見）」の概念を持ち出された点、**このシステムの最も深遠な課題を突く、極めて鋭いご指摘**です。

アリストテレスは『分析論前書』で「必然的な真理（真か偽か）」を扱う演繹（証明）を定義した一方で、『トポス論』では「蓋然的な真理（エンドクサ）」に基づく弁証法（ダイアレクティケー）を論じました。
企業内のドキュメントや日常言語の大部分は、絶対的な真理ではなく、「現在のプロジェクトではこう考えられている」「営業部としてはこれが正しい」といったエンドクサ（文脈依存の通念）で構成されています。

これを「True(1)」か「False(0)」しか許容しない厳格なOWL/推論エンジンにそのまま流し込むと、システムは「矛盾」としてフリーズするか、データの信頼性が崩壊（データスワンプ化）します。

この課題をシステム的に管理し、真偽判別（あるいは合意形成）を行うための具体的なアーキテクチャ設計を3つのレイヤーで提案します。

---

### 1. データ表現レイヤー：RDF-star（RDF*）による「ステートメントの対象化（Reification）」

命題そのものを絶対的な真理としてグラフに書き込むのではなく、**「誰が（どの文書が）、それを真だと主張しているか」というメタ命題として管理**します。
これを最新のグラフDB技術である **RDF-star (RDF*)** や Reification（具象化）を用いて実装します。

* **絶対的真理（分析論的命題）の表現:**
* `[システムA] - (is_a) -> [クラウドサービス]`
* これは誰が見ても真（事実）として直接グラフに書き込みます。


* **エンドクサ（トポス論的命題）の表現:**
* LLMが「新システムは従来の3倍効率的である」という命題を抽出したとします。
* これを直接書き込むのではなく、**命題自体を一つのエンティティとして包み込みます。**
* `<< [新システム] - (has_property) -> [高効率] >> - (stated_by) -> [営業部_提案書_v1]`
* `<< [新システム] - (has_property) -> [高効率] >> - (epistemic_status) -> [Endoxa (通念)]`



**管理方法:** 推論エンジン（Assurance Layer）には「絶対的真理」のネットワークのみで矛盾チェックを行わせ、「Endoxa」フラグが付いた命題は、チェックの対象から外して（あるいは別の緩いルールセットで）管理します。

### 2. オントロジーの分離：Core（事実）と Context（解釈）の分離

知識グラフを単一の空間で管理するのではなく、レイヤーを分けます。

* **Core Ontology（ハードな事実）:**
組織図、製品マスタ、システム構成図など、MCP経由で注入される「目的因」や「絶対的ルール」の領域。ここでは矛盾律が厳密に適用されます。
* **Contextual Ontology（ソフトなエンドクサ）:**
LLMが各ドキュメントから抽出した「意見」「評価」「仮説」の領域。ここでは「A部はシステムXを『有用』と評価し、B部は『不要』と評価している」という**矛盾が共存すること自体が正しい状態**となります。

**管理方法:** LLMが命題を抽出する際、MVP 1の機能として「この命題は Fact（事実）か、Endoxa（通念・意見）か」をフラグ付け（分類）させます。LLMはこの手の「主観・客観の分類」を非常に高い精度で行うことができます。

### 3. 真偽判別のプロセス：弁証法（Dialectic）としてのHITL（Human-in-the-Loop）

エンドクサの真偽は、システムが自動的に決定できるものではありません。アリストテレスがトポス論で説いたように、**エンドクサは他者との対話（弁証法）を通じてのみ、より高次な真理へと昇華（アウフヘーベン）されます。**

したがって、システムにおける最終的な真偽判別は、**MVP 5の「HITL（人間の承認ゲートウェイ）」を「デジタル・シンポジウム（討論の場）」として機能させる**ことで解決します。

1. **蓄積と可視化:** LLMが抽出した大量のエンドクサ（例：「このモジュールは非推奨である」）が、グラフ上に「保留（Proposed）」状態で蓄積されます。
2. **人間（ドメインエキスパート）による評価:** ダッシュボード上に、「複数のドキュメントから『このモジュールは非推奨である』というエンドクサが多数抽出されています。これを組織のCoreルール（真）として昇格させますか？」というサジェストが表示されます。
3. **真理への昇華（合意形成）:** 管理者が「承認」ボタンを押すと、そのエンドクサは `(epistemic_status: Fact)` に書き換えられ、Core Ontologyに昇格します。以降、LLMはこれを絶対的なルール（真）として扱うようになります。

---

### 結論：どう管理し、どう判別するか

「システムには真偽を判別させない（保留する）」が最適解です。

1. LLMには、抽出した命題が絶対的な事実か、文脈依存のエンドクサかを判定させ、メタデータ（`Endoxa`フラグや参照元ドキュメント）を付与して保存させます。
2. 推論エンジンは「絶対的事実」のみを用いて厳密な論理チェックを行い、エンドクサは「意見のネットワーク」として矛盾したまま共存させます。
3. 最終的に、人間のドメインエキスパートがそのエンドクサの集積を見て、「組織のルール（真）」へと手動で昇格させる（弁証法的プロセスをUIで実装する）。

これが、人間の曖昧な知識をシステム上で安全に管理し、破綻を防ぐための最高のアプローチとなります。

## 補足3. 客観的事実と通念の判定
```
LLMに命題を抽出させる際、それが客観的事実(Fact)なのか、文脈依存の通念(Endoxa)なのかを判別させるための、具体的なプロンプトとJSON出力スキーマを設計してください。
```
ご提示いただいた「Fact（客観的事実）」と「Endoxa（文脈依存の通念）」を分類し、RDF-star的なメタデータを付与して抽出するための、具体的なLLMプロンプトおよびJSON出力スキーマを設計しました。

このプロンプトの要諦は、LLMに対して「アリストテレス的オントロジーの管理者」というペルソナを与え、判断基準（FactかEndoxaか）を明確なビジネスルールとして定義することです。

---

### 1. JSON出力スキーマ（Pydanticモデル / JSON Schema想定）

システム側（LangChain等）でパースし、グラフDBやRDF-star形式に変換するための構造化スキーマです。真偽判別を後続のHITL（人間の承認プロセス）に委ねるため、`rationale`（判定理由）と`source_quote`（引用元）を必ず出力させます。

```json
{
  "type": "object",
  "properties": {
    "propositions": {
      "type": "array",
      "description": "テキストから抽出された命題（トリプル）のリスト",
      "items": {
        "type": "object",
        "properties": {
          "subject": {
            "type": "string",
            "description": "主語（実体、概念、システム名など）"
          },
          "predicate": {
            "type": "string",
            "description": "述語（is_a, has_property, utilizes, depends_on 等の標準化された関係性）"
          },
          "object": {
            "type": "string",
            "description": "目的語（実体、あるいは付帯的・状態的な値）"
          },
          "epistemic_status": {
            "type": "string",
            "enum": ["Fact", "Endoxa"],
            "description": "命題の認識論的ステータス（事実か通念か）"
          },
          "rationale": {
            "type": "string",
            "description": "なぜFactまたはEndoxaと判定したかの論理的理由（短い説明）"
          },
          "source_quote": {
            "type": "string",
            "description": "この命題の根拠となったテキストの正確な一文（RDF-starのメタデータとして使用）"
          }
        },
        "required": ["subject", "predicate", "object", "epistemic_status", "rationale", "source_quote"]
      }
    }
  }
}

```

---

### 2. システムプロンプト設計

LLMに文脈と判断基準を厳格に守らせるためのプロンプトです。Few-Shotプロンプティング（例示）を組み込むことで、分類の精度を飛躍的に高めます。

```markdown
# Role
あなたはエンタープライズ知識基盤を構築する専門的なオントロジー管理者であり、論理学者です。
入力されたテキストから知識の最小単位である「命題（主語-述語-目的語のトリプル）」を漏れなく抽出し、それぞれの命題が「客観的事実（Fact）」であるか、「文脈依存の通念・意見（Endoxa）」であるかを厳密に分類してください。

# Definitions (分類基準)
1. **Fact (客観的事実)**
   - 組織の構造、システムアーキテクチャの確定事項、物理的な事実、または変更不可能な定義。
   - 誰が読んでも真偽が揺るがない、あるいはシステム的に「絶対のルール」として扱われるべきもの。
   - 例: 「AシステムはAWS上で稼働している」「営業部は本社組織の一部である」

2. **Endoxa (通念・意見・評価)**
   - 評価、予測、特定の部門や個人の視点、推奨事項、または時間の経過や文脈によって真偽が変わり得るもの。
   - トポス論における「広く受け入れられているが、絶対的真理とは限らない意見」。
   - 例: 「Aシステムは使いにくい」「このモジュールは非推奨とするべきだ」「XプロジェクトはYプロジェクトより優先度が高い」

# Extraction Rules
- 複雑な文章は、複数のシンプルな命題（トリプル）に分解してください。
- 述語（predicate）は、可能な限り一般的な関係性（例: is_a, has_property, uses, causes, evaluated_as）に正規化してください。
- 各命題には、なぜその `epistemic_status` に分類したかの理由（`rationale`）と、抽出元の原文（`source_quote`）を必ず付与してください。

# Examples
【入力テキスト】
「次期CRMシステムはSalesforceを採用する。しかし、営業部門からは画面遷移が複雑であり業務効率が落ちるとの懸念が出ている。そのため、導入前にUIのカスタマイズが必須である。」

【出力JSON】
{
  "propositions": [
    {
      "subject": "次期CRMシステム",
      "predicate": "uses",
      "object": "Salesforce",
      "epistemic_status": "Fact",
      "rationale": "採用される技術スタックに関する決定事項・客観的事実であるため。",
      "source_quote": "次期CRMシステムはSalesforceを採用する。"
    },
    {
      "subject": "次期CRMシステムの画面遷移",
      "predicate": "has_property",
      "object": "複雑",
      "epistemic_status": "Endoxa",
      "rationale": "営業部門という特定の視点からの主観的な評価・意見であるため。",
      "source_quote": "営業部門からは画面遷移が複雑であり業務効率が落ちるとの懸念が出ている。"
    },
    {
      "subject": "UIのカスタマイズ",
      "predicate": "is_a",
      "object": "必須事項",
      "epistemic_status": "Endoxa",
      "rationale": "ある目的を達成するための文脈依存の推奨事項・方針（絶対的な自然法則ではない）であるため。",
      "source_quote": "導入前にUIのカスタマイズが必須である。"
    }
  ]
}

# Task
以下のテキストを解析し、指定されたJSONスキーマに従って命題リストを出力してください。

```

---

### 3. この設計がもたらすシステム的効果

このプロンプトとスキーマで抽出を行うことで、システム（LangGraphのワークフロー等）では以下のような美しい制御が可能になります。

1. **フィルター処理（Assurance Layerの保護）:**
後続のOWL/SHACL推論エンジンにデータを渡す際、プログラム側で `if prop.epistemic_status == "Fact":` の条件を挟みます。これにより、推論エンジンには「AWS採用」のような確定事実だけが流し込まれ、矛盾律の計算が安全に行われます。
2. **知識の保留と可視化（UIでの活用）:**
`Endoxa` として抽出された「画面遷移が複雑」「カスタマイズが必須」という命題は、ネットワーク図（ナレッジ・ビューア）上で、例えば**破線**や**警告色**のノードとして可視化できます。
3. **議論のトリガー（HITLの支援）:**
人間の管理者は、ダッシュボード上に集約された `Endoxa` のリストと `source_quote` を見て、「営業部のこの懸念（Endoxa）はもっともなので、全社の『要件（Fact）』としてオントロジーに昇格させよう」という意思決定をシステム上で行うことができます。

## 補足4. 信念改訂（Belief Revision）」や「真理維持システム（Truth Maintenance System: TMS）」

```
真偽判定を終えたものでも、新しい文章を解析した結果、判定が覆る、endoxaだったものがfactであると判明する、またその逆といったことが出てくるかと思いますが、過去の命題から更新対象の命題を探索して更新するにはどうすればよいでしょうか。
```
これは知識工学における極めて本質的な課題であり、人工知能の分野では「信念改訂（Belief Revision）」**や**「真理維持システム（Truth Maintenance System: TMS）」と呼ばれる領域のテーマです。

時間の経過や新しい情報源の登場によって、かつての「単なる噂（Endoxa）」が「公式決定（Fact）」に変わったり、逆に「事実（Fact）」とされていた古い構成が陳腐化して「過去の事実（あるいは誤り）」に覆ったりすることは、エンタープライズ環境では日常茶飯事です。

過去の命題を探索し、矛盾や状態変化を検知して安全に更新するためには、グラフデータベースとベクトル検索を組み合わせた「動的更新アーキテクチャ」を構築する必要があります。具体的なアプローチを4つのステップで解説します。

---

### 1. ベクトル検索による「類似命題」の動的探索（Detection）

新しいテキストから命題を抽出した際、URI（完全一致）だけで過去の命題を探そうとすると表記揺れで見落とします。抽出と同時に、**命題の「意味（セマンティクス）」で過去のグラフを検索する機構**が必要です。

* **実装アプローチ:**
1. LLMが抽出した新しい命題（例：「CRMはSalesforceである」）をベクトル化（Embedding）する。
2. DuckDB（あるいは専用のベクトルDB）に格納されている過去の全命題ベクトルに対して、類似度検索（Cosine Similarity）をかける。
3. 意味的に近い過去の命題（例：「次期システムにはSFDCが有力だ」）を上位数件ピックアップし、更新候補としてLLMのコンテキストに注入する。



### 2. 「情報源の権威性（Authority）」に基づく状態の再評価（Evaluation）

新しい命題と過去の命題が衝突・関連していると分かった場合、「どちらを信じるべきか（どちらがFactに昇格・降格するか）」を判定するロジックが必要です。ここではテキストの内容だけでなく、**情報源のメタデータ**が決定打となります。

* **判定マトリクスの例:**
* **Endoxa → Fact の昇格:**
過去の命題が「議事録（権威: 低）」から抽出されたEndoxaであり、新しい命題が「公式アーキテクチャ決定書（権威: 高）」から抽出されたFactである場合。公式文書が裏付けとなったため、過去のEndoxaノードを特定し、Factへ上書き（統合）する。
* **Fact → Fact（衝突・更新）:**
過去が「v1仕様書」、現在が「v2仕様書」。両方Factだが時間が新しい方を正とする。
* **Fact → Endoxa（疑義の発生）:**
絶対的ルールだと思っていた事象に対し、複数の現場ドキュメントから大量の例外（Endoxa）が抽出された場合。「この事実は実態と乖離しているのではないか？」という疑義フラグを立てる。



### 3. RDF-starを用いた「ソフトデリートとバージョン管理」（Versioning）

システム上で更新を行う際、**過去の命題を物理削除（Hard Delete）してはいけません。** 「過去のある時点では、それが真であると考えられていた」という事実自体が、重要なナレッジだからです。

* **実装アプローチ:**
RDF-starのメタデータ（プロパティグラフ機能）を利用し、命題に「有効期間」や「無効化フラグ」を付与して状態を遷移（ソフトデリート）させます。
```turtle
# 過去の命題（Endoxaから事実として確定したが、後に陳腐化した例）
<< ex:CRM ex:uses ex:SystemA >>
    ex:epistemic_status "Fact" ;
    ex:valid_from "2024-01-01" ;
    ex:invalidated_at "2026-08-27" ; # ← 新事実発覚により無効化タイムスタンプを打つ
    ex:superseded_by << ex:CRM ex:uses ex:Salesforce >> . # ← 新しい命題へのポインタ

# 新しい命題（現在のFact）
<< ex:CRM ex:uses ex:Salesforce >>
    ex:epistemic_status "Fact" ;
    ex:valid_from "2026-08-27" ;
    ex:source "公式プレスリリース_2026.pdf" .

```


これにより、推論エンジンには「現在有効なFact」のみを読み込ませつつ、グラフ上には「知識の変遷履歴」が完全に保存されます。

### 4. カスケード更新（Truth Maintenance: 真理の伝播）

これが最も難しい部分ですが、OWL推論エンジン（Assurance Layer）を使っている最大のメリットでもあります。

ある大前提（Fact）が覆った場合、**過去にその大前提から三段論法で自動生成された（推論された）子ノードの階層も、すべて自動的に再評価・破棄されなければなりません。**
推論エンジン（HermiT等）は、ベースとなるFactのセットが入れ替わった状態で再計算（Sync Reasoner）を走らせるだけで、古いFactに依存していた推論ツリーを自動的に刈り取り、新しいFactに基づいた正しい多次元ツリーを瞬時に再構築してくれます。

---

### まとめ：LangGraphにおける更新ワークフロー

これをLangGraphのサイクルに落とし込むと、以下のようになります。

1. **抽出＆検索:** LLMが新文書から命題を抽出する。同時にDuckDBでベクトル検索を行い、類似する過去の命題（Fact/Endoxa）を引っ張ってくる。
2. **LLMによる差分評価:** プロンプト内で「過去にはこう言われていたが、今回の文書を踏まえるとどう状態が変化したか？」を判断させる（`upgrade_to_fact`, `invalidate_old_fact` などのアクションタグを出力させる）。
3. **グラフ更新:** 古いRDFトリプルに無効化タイムスタンプを打ち、新しいトリプルを追記する。
4. **推論再構築:** 推論エンジンを走らせ、全体の論理整合性をチェックし直す。

## 補足5. 本ツールが対象とすべきドキュメント・データとは
```
このアプリが対象とする文章、データですが
* 過去のプロジェクトの成果物
* 問合せチケット、イシュー
などを想定しており、製品マニュアルなどの真偽が明確なものは対象外と考えていますがどうでしょうか。
製品マニュアルも命題化すべきでしょうか
```
結論から申し上げますと、**製品マニュアル等の「真偽が明確なもの」も、必ず対象として含める（命題化する）べきです。むしろ、それらこそがこのシステムの「要（アンカー）」となります。**

「真偽が明確だから対象外でよい」という直感は非常に理解できます。しかし、本アーキテクチャの特性（Neuro-Symbolic AIによる知識グラフ構築）を踏まえると、マニュアルを除外してしまうと、システム全体の推論能力と価値が半減してしまいます。

なぜマニュアルも含めるべきなのか、システム設計の観点から3つの理由を解説します。

### 1. エンドクサ（通念）を接地させる「絶対的アンカー」の役割

過去のプロジェクト成果物や問合せチケットから抽出される命題は、先ほど議論した通り、その多くが「エンドクサ（文脈依存の意見や事象）」です。
例えば、問合せチケットから以下のような命題が抽出されたとします。

* `[モジュールA] - (has_issue) - [データ同期エラー]` （Endoxa/一時的な事実）

もしシステム内に製品マニュアルが読み込まれていなければ、システムにとって「モジュールA」とは、単なる文字列に過ぎず、どこに属し、本来どう動くべきものなのかという「文脈（コンテキスト）」が全くありません。
製品マニュアルを事前に命題化しておくことで、以下のような「事実（Fact）」のネットワークが形成されます。

* `[モジュールA] - (is_part_of) - [データ連携基盤]` （Fact）
* `[モジュールA] - (requires) - [ネットワークポート8080]` （Fact）

この「揺るぎない事実の背骨（Core Ontology）」があるからこそ、チケットから抽出された曖昧な事象（Endoxa）がグラフ上の正しい位置に紐付き、意味を持つようになります。

### 2. 「あるべき姿（Fact）」と「現実（Endoxa）」のギャップ検知

製品マニュアルは「システムはこう動くべきである（全称肯定など）」という仕様（目的因）の塊です。一方、問合せチケットやイシューは「現実にはこう動いてしまった」という例外の塊です。

両方を命題化して同じ推論エンジン（Assurance Layer）に流し込むことで、**「マニュアルの定義（Fact）と、現場のチケット（Endoxa）の間に生じている矛盾」を機械的に検知できる**ようになります。

* **マニュアルの命題:** 「全ての『標準ユーザー』は『設定画面』にアクセスできない（全称否定）」
* **チケットの命題:** 「ある『標準ユーザー』が『設定画面』のバグを報告してきた（特称肯定）」

推論エンジンは即座にここで「矛盾律違反」を検知します。これにより、人間は「マニュアルの記述が古いのか？」あるいは「重大なセキュリティバグ（権限漏れ）が発生しているのか？」という、極めて高度なインサイトを得ることができます。マニュアルがなければ、この矛盾を計算することができません。

### 3. MCPの「グラウンディング用データ」の自動生成

以前の議論で、LLMのハルシネーションを防ぐために、MCP経由で組織のマスターデータや既存ルール（前提知識）を注入するアプローチを定義しました。
製品マニュアルを本システムに読み込ませて命題化することは、まさに「MCPが提供するためのグラウンディング用データを、手作業ではなく自動で構築する」という最強のアプローチになります。

### 実装上のアプローチ（ソースによる処理の分岐）

対象とする文書の性質（真偽の明確さ）が異なるため、システム内での「扱い方（パイプライン）」を分岐させるのがベストプラクティスです。

* **製品マニュアル（真偽が明確な公式文書）**
* **処理フロー:** 抽出された命題は、LLMに分類させるまでもなく、デフォルトで `epistemic_status: "Fact"` として付与します。
* **承認プロセス:** 人間の承認（HITL）をスキップし、即座にグラフDB（Core Ontology層）に「絶対的真理」として書き込みます。


* **過去の成果物・問合せチケット（真偽が曖昧な現場文書）**
* **処理フロー:** 前述のプロンプトを用いて、LLMに `Fact` か `Endoxa` かを慎重に推論・分類させます。
* **承認プロセス:** マニュアルから生成されたFactを「前提知識」として参照しながら論理チェックを行い、未知のものや矛盾するものは、人間の承認ゲートウェイ（HITL）で評価（Contextual Ontology層）します。



結論として、製品マニュアルのような公式ドキュメントは、この多次元概念アーキテクチャにおいて「北極星（正しい方向を示す基準）」として機能します。ぜひ、プロジェクトの初期段階（MVPのナレッジ構築フェーズ）で、最優先でインジェスト（読み込み・命題化）する対象に含めることを強くお勧めします。

## 補足6. このツールの使い方
```
このツールの使い方として、
1.  ユーザーが質問文を入力する。
2. ツールは、ユーザーの質問にマッチする命題を検索する。
3. 命題データに基づいて、情報の確度(事実なのか、〇〇さんが言ってたのでおそらく正しい、など）とともに回答を返す
といったものでしょうか
```
ご認識の通りです！まさにその3ステップが、本システムがエンドユーザー（検索者や業務担当者）に提供する究極の価値（フロントエンドの検索体験）となります。

これまで議論してきた複雑なバックエンド（命題化、推論エンジン、Fact/Endoxaの分離、RDF-star）は、すべて「ユーザーに対して、単なる文字列ではなく『情報の確度（認識論的ステータス）』を伴った誠実な回答を返す」という、このフロントエンドの体験を実現するために存在しています。

従来のRAG（検索拡張生成）システムと比較しながら、ご提示いただいた3つのステップがシステム内部でどのように動くかを解説します。

### 従来のRAGと本ツールの決定的な違い

通常のRAGシステムに「新システムの仕様について教えて」と質問すると、製品マニュアル（事実）と過去のクレームチケット（意見）をごちゃ混ぜにして読み込み、「仕様は〇〇ですが、使えないシステムです」といった、事実と主観が混ざったハルシネーション（もっともらしいウソ）を平然と返してきます。

一方、本ツール（多次元概念アーキテクチャ）では、以下のような高度な回答フローが実現します。

#### 1. ユーザーの質問入力

* **ユーザー:** 「モジュールAのデータ同期エラーについて、原因と対処法を教えてください」

#### 2. ツールによる命題グラフの探索（Vector + Graph Search）

* ツールは質問をベクトル化し、グラフDB（DuckDB / RDFストア）を探索します。
* ここでツールは、単なる「関連する文章」ではなく、「Factの背骨」と「Endoxaの葉」の繋がり（ネットワーク）を引っ張ってきます。
* `[モジュールA]` - `(requires)` -> `[ポート8080]` **（ステータス：Fact / 出典：公式マニュアル v2）**
* `[モジュールA]` - `(has_issue)` -> `[同期エラー]` **（ステータス：Endoxa / 出典：チケット #105, #108）**
* `[同期エラー]` - `(caused_by)` -> `[ファイアウォールの設定]` **（ステータス：Endoxa / 出典：〇〇さんの日報）**



#### 3. 「情報の確度」と「情報源」を伴う回答の生成

* LLMは、取得したグラフ構造とメタデータ（`epistemic_status` や `source_quote`）をもとに、事実と意見を明確に切り分けた回答を生成します。

> **【本ツールの回答イメージ】**
> モジュールAのデータ同期エラーについて回答します。
> **【公式の仕様（Fact）】**
> 製品マニュアル（v2）によると、モジュールAの同期には「ポート8080の開放」が必須要件として定義されています。
> **【現場の報告・仮説（Endoxa）】**
> 過去の問合せチケット（#105, #108）にて同様のデータ同期エラーが報告されています。
> 〇〇さんの日報（2026年8月）の分析によれば、「顧客環境のファイアウォール設定でポート8080が塞がれていること」が原因である**可能性が高い（現場の通念）**とされています。
> **【推奨アクション】**
> 公式要件（Fact）と現場の報告（Endoxa）を照らし合わせると、まずは顧客のファイアウォール設定を確認することを推奨します。

---

### 結論：システムのフロントエンドとしての完成形

ご提示いただいた、「命題データに基づいて、情報の確度とともに回答を返す」というUX（ユーザー体験）こそが、アリストテレスの『トポス論』を現代のシステムに実装する最大の理由です。

「絶対的な事実（マニュアル）」と「蓋然的な通念（チケット・日報）」をシステムが正確に区別し、ユーザーにそのギャップを提示することで、ユーザーは「システムが嘘をついているかもしれない」という疑心暗鬼から解放され、提示された『確度』をもとに正しい意思決定を下すことができるようになります。

## 補足7. 回答生成プロンプト
```
検索フェーズにおいて、グラフから取得したFactとEndoxaのリストをLLMに渡し、情報の確度を明示した回答を生成させるための「回答生成用プロンプト」を設計したいです。
```
グラフデータベース（DuckDBやRDFストア）から取得した「事実（Fact）」と「通念・仮説（Endoxa）」のリストをLLMに渡し、情報の確度と情報源を明確に切り分けた回答を生成させるためのシステムプロンプトを設計しました。

このプロンプトの最大の目的は、**LLMに事実と主観を絶対に混同させず、両者のギャップ（仕様と現実のズレ）をユーザーに論理的に提示させること**です。

---

### 回答生成用システムプロンプト

```markdown
# Role
あなたはエンタープライズ知識基盤の高度な回答生成エージェントです。
ユーザーの質問に対し、検索システムから渡された「命題データ（事実と通念）」のみを根拠として、情報の「確度」を厳密に区別した誠実で論理的な回答を作成してください。

# Definitions (情報の確度について)
システムから渡されるデータには、以下の2つのステータス（確度）が存在します。
1. **Fact（客観的事実）**: 製品マニュアル、公式仕様、組織の絶対的なルール。
2. **Endoxa（現場の通念・仮説）**: 過去の問合せチケット、日報、個人の見解や一時的な事象。

# Rules
回答を生成する際は、以下のルールを厳守してください。
1. **確度の明確な分離**: FactとEndoxaを絶対に混ぜて話さないでください。Factは「仕様として定義されています」「〜です」と断定し、Endoxaは「〜という報告があります」「〇〇氏によれば〜の可能性があります」と、推測や伝聞のトーンを用いてください。
2. **情報源（Source）の明示**: 各情報の文末や文中に、必ず提供されたデータの出典（チケット番号、マニュアル名など）を明示してください。
3. **ギャップの提示**: もしFact（本来の仕様）とEndoxa（現場の事象）の間に矛盾やズレがある場合は、それを「留意点」としてユーザーに強調して伝えてください。
4. **ハルシネーションの禁止**: 提供された命題データセットに存在しない情報は絶対に捏造せず、情報が不足している場合は「提供されたデータからは分かりません」と回答してください。

# Output Format
回答は以下のMarkdown構成に沿って出力してください。

1. **【結論】**: 質問に対する端的な回答。
2. **【公式の仕様・事実】**: Factデータに基づく回答（該当データがない場合は省略）。
3. **【現場の報告・仮説】**: Endoxaデータに基づく回答（該当データがない場合は省略）。
4. **【総合的な推察とアクション】**: FactとEndoxaを照らし合わせた上での、推奨されるネクストアクションや留意点。

---
# Input Data
【ユーザーの質問】
{user_query}

【取得された命題データセット（JSON形式）】
{retrieved_propositions_json}

```

---

### プロンプトへの入力イメージ（Python実装時のデータ渡し）

LangChain等のテンプレート変数 `{retrieved_propositions_json}` には、ベクトル検索とグラフ検索を組み合わせて取得した以下のようなJSONリストをそのまま注入（Inject）します。

```json
[
  {
    "subject": "モジュールAのデータ同期",
    "predicate": "requires",
    "object": "ポート8080の開放",
    "epistemic_status": "Fact",
    "source_quote": "データ同期機能を利用する場合、ポート8080を開放してください。",
    "document": "公式マニュアル_v2.pdf"
  },
  {
    "subject": "同期エラー",
    "predicate": "caused_by",
    "object": "顧客FWでのポート遮断",
    "epistemic_status": "Endoxa",
    "source_quote": "おそらく顧客FWでポートが塞がれていたのが原因だと思われる。",
    "document": "問合せチケット_#105"
  }
]

```

### 4. 運用上の補正事項：確度・根拠・推論の分離

補足6・7の設計を実運用に適用する際は、`Fact` / `Endoxa` と「確度」を同一の軸として扱わないことが重要です。`Fact` は命題の扱い（仕様、定義、観測事実など）を表し、`Endoxa` は意見、評価、仮説、局所的な報告など、文脈に依存する命題を表します。一方、確度は情報源の権威性、根拠の種類、独立した情報源の数、鮮度、承認状態などから別に評価します。

例えば、公式マニュアルは「公式仕様として定義されている」という意味では強い根拠ですが、現行システムが実際にその通り動作することや、記載内容が最新であることを自動的に保証するものではありません。したがって、公式文書の命題も「客観的真理」ではなく、少なくとも「ある時点における公式仕様」として管理します。

命題データには、次のような属性を追加することを推奨します。

```json
{
  "proposition_id": "prop-000105",
  "epistemic_status": "Fact",
  "claim_type": "specification",
  "source_authority": "authoritative",
  "support_level": "direct_quote",
  "confidence": 0.95,
  "evidence_count": 1,
  "independent_source_count": 1,
  "valid_time": {
    "from": "2026-01-01",
    "to": null
  }
}
```

ここで `confidence` はLLMの主観的な確信度だけで決めず、情報源のメタデータや検証結果から算出します。また、`Endoxa` を常に「推測」と表現してはいけません。命題の種類に応じて、以下のように表現を分けます。

* 発生報告：「チケット105で報告されています」
* 評価：「営業部では使いにくいと評価されています」
* 仮説：「原因である可能性が示されています」
* 推奨：「対応案として提案されています」

### 5. 回答中の主張・推論・推奨の分離

回答生成では、検索結果に直接含まれる主張と、LLMが複数の主張から導いた推論と、利用者に提案するアクションを別種の情報として扱います。

* `retrieved_claim`: 検索された命題。必ず `proposition_id` と出典を示す。
* `derived_inference`: 命題から導いた推論。使用した命題IDを示し、事実として断定しない。
* `recommended_action`: 推奨する確認・対応。根拠となった命題IDを示す。
* `unknown`: 提供された命題から判断できない事項。

そのため、「総合的な推察とアクション」は有用な出力ですが、検索データそのものではありません。例えば「まず顧客のファイアウォール設定を確認する」は、Factではなく、FactとEndoxaを組み合わせた推奨です。回答では、その区別が読者に分かるように表示します。

### 6. 取得データの未信頼性と引用検証

`source_quote`、文書名、チケット本文など、検索で取得したデータはLLMへの命令ではなく、未信頼の引用対象データとして扱います。取得データ内に命令文やプロンプトが含まれていても、それを実行せず、命題の根拠としてのみ処理する規則を回答生成プロンプトに追加します。

出典は表示名だけでなく、安定した `document_id`、`proposition_id`、ページ番号やチケット番号などの位置情報を保持します。生成後の回答については、引用された命題IDが実際の取得データに存在するか、引用文が原文と一致するかをプログラム側で検証します。

### 7. Fact / Endoxa の昇格と回答時の扱い

`Endoxa` を検索時に自動的に `Fact` へ昇格させることは避けます。複数の独立した報告が集まった場合でも、それは「証拠が強くなったEndoxa」または「Fact候補」であり、承認ゲートを通過するまでは文脈依存の命題として提示します。昇格後も、元の出典、承認者、承認日時、適用範囲を履歴として保持します。

また、Fact同士またはFactとEndoxaの不一致は、直ちに論理矛盾と断定しません。適用範囲、対象バージョン、時間、環境が異なる可能性を確認した上で、次のような状態に分類します。

* 仕様と実態の差異
* 旧版と新版の差異
* 適用範囲の差異
* 同一条件下での未解決の矛盾

### 8. 補足6・7と現在のPoCの範囲

現在のPoCは、命題抽出、Fact / Endoxa分類、source priority、バージョン履歴、RDF / JSON変換までを検証するものです。質問のベクトル検索、グラフ検索、回答生成、引用の自動検証、ギャップ検出は次段階の実装対象です。

したがって、補足6・7に記載した回答体験は完成済みの機能ではなく、これらの検索・生成コンポーネントを追加した際の目標仕様として位置付けます。

### このプロンプト設計がもたらす効果

1. **トーン＆マナーの強制（Stylistic Control）**
プロンプト内で「Factは断定」「Endoxaは伝聞・推測」と言葉遣いを具体的に指示することで、LLM特有のもっともらしい断言（ハルシネーション）を文体レベルで抑制します。
2. **メタデータの自然な言語化（Natural Language Generation）**
裏側のRDF-starやDuckDBで管理されていた `epistemic_status`（認識論的ステータス）という機械的なフラグを、LLMが「確度」という文脈に翻訳し、ユーザーが読みやすいレポート形式に変換します。
3. **アブダクション（仮説形成）の支援**
【総合的な推察とアクション】のセクションを設けることで、LLMは演繹的な事実（Fact）と帰納的な事象（Endoxa）を比較し、「仕様はこうだが、現場ではこうなっているため、まずはここを確認すべき」というアブダクション（最良の仮説の導出）を行ってくれます。これが、単なる検索システムを超えた「ナレッジアシスタント」としての価値を生み出します。

## 補足8. 源泉データの取り扱い
```
このツールに取り込んだ元ドキュメントの扱いについて

このツールでは、製品マニュアルや過去プロジェクトの成果物など、様々なデータを源泉として使用する想定ですが、
このツールにデータを取り込んだ後は元データは削除またはツールからの参照を解除しても問題ないですか。
```
結論から申し上げますと、元データ（元ドキュメント）の削除やツールからの参照解除は、原則として避けるべき（強く非推奨）です。ただし、法令・契約・個人情報保護上の削除要求がある場合は、保持期間、アクセス権、匿名化、削除証跡を定義した上で例外として扱います。

一見すると、すべての知識が「命題（トリプル）」としてグラフ化されれば、元のテキストは不要になるように思えます。しかし、Neuro-Symbolicアーキテクチャやエンタープライズ知識基盤において、元ドキュメントを切り離してしまうと、システムの信頼性と拡張性が根本から崩壊してしまいます。

その重大な理由を、アーキテクチャと知識工学の観点から3つ解説します。

### 1. 命題化は「不可逆圧縮」である（コンテキストの喪失）

LLMによる命題化（トリプル抽出）は、テキストから論理的な背骨を抜き出す作業ですが、これは本質的に「情報の不可逆圧縮」です。

* **削ぎ落とされるもの:** 前後の微妙なニュアンス、図表、表形式のデータ、感情的なトーン、あるいは「まだシステムが抽出すべきと認識していなかった付随情報」。
* **問題点:** ユーザーが「モジュールAの同期エラー（Endoxa）」という命題の回答を受け取った際、「もっと具体的なエラーログの形式を知りたい」と深掘りしたくなった場合、トリプルだけでは限界があります。元ドキュメントへのリンク（参照）が残っていれば、ユーザーは即座に元の問合せチケットの原文や添付画像を確認し、問題解決に進むことができます。

### 2. トレーサビリティ（証跡）とHITLプロセスの崩壊

本ツールでは、命題が「客観的事実（Fact）」か「現場の通念（Endoxa）」かを区別し、Endoxaについては人間（HITL: Human-In-The-Loop）が後から評価・承認するプロセスを設計しました。

* **問題点:** もし元ドキュメントが削除されていたり参照が切れていると、承認を行う人間（管理者）は、**「LLMが抽出した短い命題と引用文（source_quote）」だけで、それが組織の正式なルールに昇格してよいかを判断しなければなりません。**
* これは極めて危険です。前後の文脈（どんなトラブルの中で書かれたチケットなのか、誰が起案したプロジェクトの成果物か）を辿る「トレーサビリティ」が失われると、管理者は自信を持って承認（Factへの昇格）ができなくなります。

### 3. AIの進化に伴う「再処理（Re-parsing）」が不可能になる

生成AIの技術進化は非常に速く、またシステムの運用を続ける中で「プロンプト（抽出ルール）を改善したい」という要件は必ず発生します。

* **問題点:** 「過去のプロジェクト成果物」からより深い関係性（新しい述語）を抽出できるようになった際、元ドキュメントが失われていると、**過去のデータに対して新しい抽出ルールを適用して知識グラフをアップデート（再構築）することが不可能**になります。

---

### 推奨されるアーキテクチャ：SSOTとインデックスの分離

したがって、本ツールにおけるデータの理想的な配置（アーキテクチャ）は、以下のようになります。

1. **元データの保管庫（Single Source of Truth: SSOT）**
* 製品マニュアル、チケット、成果物などの実体は、AWS S3、SharePoint、あるいは既存のチケット管理システム（Jira等）に**原本としてそのまま保存**しておきます。


2. **知識グラフ（本ツール）の位置づけ**
* 本ツール（DuckDBやRDFストア）は、原本そのものを内包するのではなく、「セマンティック・インデックス（意味的な検索目次）」として振る舞います。
* 抽出された命題データには、必ず `source_uri`（例：`[https://jira.corp.com/ticket/105](https://jira.corp.com/ticket/105)` や `s3://docs/manual_v2.pdf`）を持たせます。



**運用上のルール：**
本ツールにデータを取り込んだ後も、元データは削除せず、本ツール上の命題は「常に元データへのリンク（URI）を保持し続ける」設計にすることで、論理的な推論スピードと、原文が持つ豊かなコンテキストの両立が可能になります。

なお、命題側には `document_id`、`source_uri`、取得日時、文書版、ページやチケット番号などの位置情報を保持します。原本を削除する必要がある場合でも、削除理由、削除日時、削除対象の識別子を監査可能な形で残し、参照不能になった命題を「根拠確認不可」として回答対象から制御します。

## 補足9. 源泉データの場所
```
現在は、源泉データとしてはファイルシステム上のファイルを想定していますが、これを今後SnowflakeやDenodoなどのDWH、データ仮想化製品のデータを源泉として扱うことは可能？
```
結論から申し上げますと、**可能です。ただし、接続先を増やすだけでは不十分であり、データの意味、権限、鮮度、系譜を命題のメタデータとして管理する必要があります。**

ファイルシステム上の非構造化ドキュメント（テキストやPDF）からの命題化は、AIの自然言語処理（NLP）能力の限界に挑む難易度の高いタスクですが、DWHやデータ仮想化基盤がすでに導入されている環境であれば、システム構築のアプローチは劇的に洗練され、強固になります。

その理由と、具体的な統合アーキテクチャの設計について解説します。

### 1. 構造化データとオントロジーの圧倒的な親和性

SnowflakeのテーブルやDenodoで定義されたビューは、それ自体がすでに組織の「概念モデリング」の結果です。

* **テーブル / ビュー：** エンティティまたはデータ提供対象の候補に相当。
* **カラム（列）：** そのエンティティの属性候補に相当。
* **主キー：** エンティティを識別するための識別子候補に相当。
* **外部キー / JOIN条件：** `references`、`belongs_to`、`assigned_to` などの関連候補に相当する。`is-a` とは自動的に解釈しない。

DDLやデータカタログから得られるスキーマ・制約情報は `StructuralFact` の候補として高い確度で扱えます。一方、テーブルの行データは `MasterFact`、`ObservedFact`、`DerivedFact` などに分け、取得日時、鮮度、データ品質、更新処理の系譜を付与します。DWH由来であることだけを理由に、すべてを無条件のFactや絶対的真理として扱ってはいけません。

命題の種類は、例えば次のように管理します。

```text
スキーマ・制約情報  -> StructuralFact
マスターデータ       -> MasterFact
業務上の観測値       -> ObservedFact
集計・計算結果       -> DerivedFact
現場文書の主張       -> Endoxa / Claim
```

### 2. Denodo / Snowflakeを統合するアーキテクチャ設計

これらのエンタープライズ・データ基盤を統合する場合、アーキテクチャの中心には**MCP（Model Context Protocol）を活用したデータ統合戦略**を配置するのが最適です。

特にDenodoやUnity Catalogのようなセマンティックレイヤーが既に存在する場合、AIエージェントとそれらを直接連携させることで、極めて強力な「前提知識の注入基盤」が完成します。

* **許可済みツールによる動的アクセス:**
MCPサーバーを用いて、Denodo上のビューやSnowflakeのクエリ機能をラップします。ただし、LLMに自由なSQLの生成・実行権限を与えず、許可済みビュー、読み取り専用接続、パラメータ化クエリ、`LIMIT`、タイムアウト、コスト上限、行レベル権限、個人情報のマスキングを強制します。実行者、クエリ、取得範囲、結果の系譜は監査ログに記録します。
* **データの性質に応じた利用:**
スキーマ情報は `StructuralFact` の候補、管理済みマスターデータは `MasterFact` の候補として扱います。実測値、集計値、過去スナップショットは、それぞれの時点と品質を保持した上で、回答や推論に利用します。DenodoやSnowflakeから取得したという理由だけで、無条件に `epistemic_status: "Fact"` としてグラフへ登録しません。

### 3. 実装に向けた技術的検証ステップ

将来的なDWH/データ仮想化基盤への移行を見据える場合、システム開発のプロセスにおいて、以下のような段階的な技術検証（Technical Verification）のステージを設計に組み込むことが成功の鍵となります。

1. **接続・権限・ツールの検証:** MCPを介して、許可済みのDenodoやSnowflakeビューへ安全に認証し、意図したパラメータで読み取り専用クエリを実行できるかを確認する。自由なSQL実行ではなく、ツールの許可範囲と権限境界を検証する。
2. **オントロジー・マッピング検証:** RDBのスキーマ（リレーショナルモデル）を、自動的にRDF/OWLのプロパティ（グラフモデル）に変換するロジックの確立。
3. **ハイブリッド名寄せ（エンティティ解決）の検証:** ファイルシステム（チケットや成果物）からLLMが抽出した曖昧なエンドクサと、DWHから取得した厳密なマスタデータ（Fact）を、グラフDB上で同一のエンティティとして正しく名寄せ（紐付け）できるかのテスト。

4. **鮮度・系譜・品質の検証:** 取得日時、更新日時、スナップショット、変換ジョブ、データ品質指標を命題に紐付け、現在の値と過去時点の値を混同しないことを確認する。

この統合が実現すれば、**「DWH上の確固たる業務データ（Fact）」と、「ファイルシステム上の現場の生々しい課題や文脈（Endoxa）」がグラフ上で完全に交差する**ことになります。これこそが、エンタープライズ情報アーキテクチャの歴史的課題を解決する次世代のセマンティックレイヤーの姿です。